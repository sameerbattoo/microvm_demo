# Proxy server dependencies (runs on your machine, NOT inside the MicroVM)

# Web framework
fastapi>=0.139.0
uvicorn>=0.51.0

# HTTP client (for forwarding requests to MicroVMs)
httpx>=0.28.1

# AWS SDK (for Lambda MicroVM API, Bedrock, S3, DynamoDB, Athena, Glue)
boto3>=1.43.51

# AI Agent framework (Strands Agents SDK — powers the notebook AI assistant)
strands-agents>=1.48.0

# WebSocket relay for terminal access
websockets>=13.0

# Lightweight sample profiling for global data-source entity discovery
# (proxy.notebook.ai.entity_discovery) — profiles small samples of S3/Athena/
# DynamoDB sources directly in the proxy process, no MicroVM required.
pandas>=2.0.0
pyarrow>=14.0.0
